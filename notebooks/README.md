# Colab Notebooks

Use this folder for experimentation and module-specific Colab notebooks. When a component is stable, extract reusable functions into the appropriate `backend/` module instead of leaving production logic only inside a notebook.
